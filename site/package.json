{
  "name": "agentaction-site",
  "version": "0.1.0",
  "private": true,
  "description": "The public marketing site for AgentAction — agentaction.online.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs --check",
    "dev": "node serve.mjs",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
